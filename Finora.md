# FINORA AI — MVP 01

This is the first visual MVP: a premium responsive landing page for FINORA AI.

## Run locally
Open `index.html` in a browser.

## Next build steps
1. Move to Next.js.
2. Connect Supabase Auth + PostgreSQL.
3. Add the real AI API through a server-side endpoint (never expose the API key in browser code).
4. Add live market-data integration.
5. Add a protected dashboard and admin area.
6. Add payments only after the product/legal requirements are defined.

The current version uses no paid infrastructure.
